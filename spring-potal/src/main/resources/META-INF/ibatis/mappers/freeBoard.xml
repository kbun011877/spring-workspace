<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE sqlMap PUBLIC "-//iBATIS.com//DTD SQL Map 2.0//EN"
	"http://ibatis.apache.org/dtd/sql-map-2.dtd">
	
<sqlMap namespace="FreeBoard">
	
	<select id="getAllFreeBoard" resultClass="FreeBoard">
		select 
			board_no as no, 
			board_title as title, 
			board_writer as writer, 
			board_contents as contents, 
			board_filename1 as filename1, 
			board_filename2 as filename2, 
			board_filename3 as filename3, 
			board_create_date as createDate
		from 
			portal_free_boards

	</select>
	
	<insert id="insertFreeBoard" parameterClass="FreeBoard">
		insert into 
	</insert>
	
	
</sqlMap>