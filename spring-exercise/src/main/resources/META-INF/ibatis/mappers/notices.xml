<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE sqlMap PUBLIC "-//iBATIS.com//DTD SQL Map 2.0//EN"
	"http://ibatis.apache.org/dtd/sql-map-2.dtd">
	
<sqlMap namespace="Notice" >
	
	<select id="getRandomNotice" resultClass="Notice">
	select 
		notice_no as no,
		notice_title as title,
		notice_contents as contents,
		notice_create_date as createDate
	from
		(select 
			*
		from
			spring_notices
		order by
			dbms_random.random()) 
	 where rownum &lt;= 3
	</select>
	 
	 <select id="getAllNotice" resultClass="Notice">
	 	select 
			notice_no as no,
			notice_title as title,
			notice_contents as contents,
			notice_create_date as createDate
		from
			spring_notices
	 </select>
	 
</sqlMap>